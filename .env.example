# For Docker: prefer a single ../.env at the digital-id repo root (see digital-id/.env.example)
# and symlink: ln -sf ../.env .env
#
# For ./mvnw without Docker, copy this file to .env and match MySQL host/port.

DB_PASSWORD=change_me_local_dev
DB_NAME=digital_id
DB_USERNAME=root

JWT_SECRET=local_dev_jwt_secret_change_me_to_something_at_least_32_chars

MAIL_USERNAME=
MAIL_PASSWORD=

VITE_API_BASE_URL=http://localhost:8080
